# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Reference/planning material, not website assets
*.pdf

# n8n workflow exports - local-only, shared directly between team members
# (not through git); the README explaining them stays tracked.
automation/n8n-workflows/*.json

# OpenMind FAQ Bot - a separate standalone project (its own server, own
# node_modules, own real OpenAI API key in server/.env) that happens to sit
# in this folder. Not part of the website's own source - never commit it
# here, same reasoning as the n8n workflow files above.
OpenMind FAQ Bot/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
